VITE_SUPABASE_PROJECT_ID=your-project-id
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_LOGO_DEV_KEY=
GEMINI_API_KEY=
# Optional. Overrides the LinkedIn OAuth redirect URI. If unset, the app uses
# `${window.location.origin}/auth/linkedin/callback` automatically. Must match
# an Authorized redirect URL in the LinkedIn Developer Portal exactly.
VITE_LINKEDIN_REDIRECT_URI=
